# Opsworks attributes : tcp port, elasticsearch host

input {
	tcp {
		type => "json"
		port => <%= node["logstash-tcp-port"] %>
	}
}

output {

	elasticsearch {
		host => <%= node["es-host"] %>
	}
	stdout {
		codec => rubydebug
	}
}